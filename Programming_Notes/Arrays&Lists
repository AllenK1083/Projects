Javascript
let variable = []

variable.pop() this will remove the last element from the array
variable.push(value) this will add an element to the end of the array
variable.shift() this will remove the first element from the array
variable.unshift(value) this will add an element to the beginning of the array
variable.length this will return the length of the array
variable.splice(index,quantity,replacement value) this will remove and replace elements in the array based on index, how many CSSFontFeatureValuesRule, and with what value.
variable.indexOf(searchElement, from indexOf) this will return the index of the value
variable.filter(callback, thisArg) this will filter the array
variable.map(callback, thisArg) this will map the array
variable.reduce(callback, initialValue) this will reduce the array
variable.reduceRight(callback, initialValue) this will reduce the array

python 
variable = []
variable.append(value) this will add a value to the end of the array
variable.remove(value) this will remove the value from the array
variable.index(value) this will return the index of the value
variable.count(value) this will return the number of times the value appears in the array
variable.sort(key=None, reverse=False) this will sort the array
variable.reverse() this will reverse the array
variable.copy() this will copy the array
variable.insert(index, element) Inserts an element at a given position (index) in the list.

